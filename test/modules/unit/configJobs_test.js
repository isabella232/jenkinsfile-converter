const expect = require('chai').expect;

const { Workflow } = require('../../../model/workflow.js');
const { isPotentialError, writeJobSteps, writeJobs } = require('../../../util/configJobs.js');

describe('configJobs', () => {
  
  describe('#isPotentialError', () => {
    it('needs tests', () => {
      expect('foo'
        ).to.be.a('string');
    });
  });

  describe('#writeJobSteps', () => {
    it('needs tests', () => {
      expect('foo'
        ).to.be.a('string');
    });
  });

  describe('#writeJobs', () => {
    it('needs tests', () => {
      expect('foo'
        ).to.be.a('string');
    });
  });
  
});
