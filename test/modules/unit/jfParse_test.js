const expect = require('chai').expect;

const { 
  pullDirective, 
  checkDirective, 
  isBalanced, 
  removeComments, 
  jenkinsfileToArray, 
  getBalancedIndex, 
  getSection } = require('../../../util/jfParse.js');

describe('jfParse', () => {
  
  describe('#pullDirective', () => {
    it('needs tests', () => {
      expect('foo'
        ).to.be.a('string');
    });
  });

  describe('#checkDirective', () => {
    it('needs tests', () => {
      expect('foo'
        ).to.be.a('string');
    });
  });

  describe('#isBalanced', () => {
    it('needs tests', () => {
      expect('foo'
        ).to.be.a('string');
    });
  });

  describe('#removeComments', () => {
    it('needs tests', () => {
      expect('foo'
        ).to.be.a('string');
    });
  });

  describe('#jenkinsfileToArray', () => {
    it('needs tests', () => {
      expect('foo'
        ).to.be.a('string');
    });
  });

  describe('#getBalancedIndex', () => {
    it('needs tests', () => {
      expect('foo'
        ).to.be.a('string');
    });
  });

  describe('#getSection', () => {
    it('needs tests', () => {
      expect('foo'
        ).to.be.a('string');
    });
  });

});
