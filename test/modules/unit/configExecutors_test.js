const expect = require('chai').expect;

const { Workflow } = require('../../../model/workflow.js');
const { writeExecutors } = require('../../../util/configExecutors.js');

describe('Executors', () => {
  
  describe('#writeExecutors', () => {
    it('needs tests', () => {
      expect('foo'
        ).to.be.a('string');
    });
  });

});
