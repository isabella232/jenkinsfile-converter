const expect = require('chai').expect;

const { Pipeline, Workflow, Job, Step, Comment } = require('../../../model/workflow.js');

describe('Models', () => {
  
  describe('#Pipeline', () => {
    it('needs tests', () => {
      expect('foo'
        ).to.be.a('string');
    });
  });

  describe('#Workflow', () => {
    it('needs tests', () => {
      expect('foo'
        ).to.be.a('string');
    });
  });

  describe('#Job', () => {
    it('needs tests', () => {
      expect('foo'
        ).to.be.a('string');
    });
  });

  describe('#Step', () => {
    it('needs tests', () => {
      expect('foo'
        ).to.be.a('string');
    });
  });

  describe('#Comment', () => {
    it('needs tests', () => {
      expect('foo'
        ).to.be.a('string');
    });
  });

});
